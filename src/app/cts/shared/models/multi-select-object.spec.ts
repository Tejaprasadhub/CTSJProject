import { MultiSelectObject } from './multi-select-object';

describe('MultiSelectObject', () => {
  it('should create an instance', () => {
    expect(new MultiSelectObject()).toBeTruthy();
  });
});
